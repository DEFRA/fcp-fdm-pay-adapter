{
  "name": "fcp-fdm-pay-adapter",
  "version": "0.0.0",
  "description": "fcp-fdm-pay-adapter",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development npm run server:watch",
    "dev:debug": "NODE_ENV=development npm run server:debug",
    "docker:build": "docker compose build",
    "docker:dev": "docker compose up",
    "git:pre-commit-hook": "npm run lint:fix",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "postinstall": "npm run setup:husky",
    "server:debug": "nodemon --inspect-brk=0.0.0.0 --ext js,json --legacy-watch ./src",
    "server:watch": "nodemon --inspect=0.0.0.0 --ext js,json --legacy-watch ./src",
    "setup:husky": "node -e \"try { (await import('husky')).default() } catch (e) { if (e.code !== 'ERR_MODULE_NOT_FOUND') throw e }\" --input-type module",
    "start": "NODE_ENV=production node --use-strict .",
    "test": "TZ=UTC vitest run --coverage",
    "test:watch": "TZ=UTC vitest",
    "docker:test": "npm run lint && mkdir -p -m 777 coverage 2>/dev/null || true && docker compose -p fcp-fdm-pay-adapter-test down -v && docker compose -f compose.yml -f compose.test.yml run --build --rm fcp-fdm-pay-adapter",
    "docker:test:watch": "docker compose -p fcp-fdm-pay-adapter-test down -v && docker compose -f compose.yml -f compose.test.yml -f compose.test.watch.yml run --build --rm fcp-fdm-pay-adapter"
  },
  "author": "Defra DDTS",
  "contributors": [
    "John Watson <john.watson1@defra.gov.uk>"
  ],
  "license": "OGL-UK-3.0",
  "dependencies": {
    "@aws-sdk/client-sns": "3.917.0",
    "@azure/service-bus": "7.9.5",
    "@defra/hapi-tracing": "1.22.0",
    "@elastic/ecs-pino-format": "1.5.0",
    "@hapi/hapi": "21.4.0",
    "@hapi/inert": "7.1.0",
    "@hapi/vision": "7.0.3",
    "@smithy/node-http-handler": "4.4.3",
    "aws-embedded-metrics": "4.2.0",
    "aws4": "1.13.2",
    "convict": "6.2.4",
    "convict-format-with-validator": "6.2.0",
    "global-agent": "3.0.0",
    "hapi-pino": "13.0.0",
    "hapi-pulse": "3.0.1",
    "joi": "17.13.3",
    "mongodb": "6.17.0",
    "pino": "10.0.0",
    "pino-pretty": "13.0.0",
    "undici": "7.18.2",
    "ws": "8.19.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.4",
    "eslint": "9.29.0",
    "husky": "9.1.7",
    "neostandard": "0.12.1",
    "nodemon": "3.1.10",
    "vitest": "3.2.4"
  }
}
